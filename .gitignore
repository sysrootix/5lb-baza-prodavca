# Raw internal source documents — must NOT be published or pushed.
# The site is served from this repo via GitHub Pages, and Pages exposes
# every committed file at the public URL. Keep the .docx local only.
docs/
# Internal bonus-system audit: contains backend architecture and live secrets.
BONUS_PROGRAM_FULL_DOCS.md

# OS / editor junk
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/
